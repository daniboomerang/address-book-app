import React from 'react';

/**
 * Adreess book home page
 * It retrieves the users from from the https://randomuser.me API and renders the users grid
 * It provides a search in order to filter results by first and last name
 */
const Home = () => (
  <div className="w-full h-full justify-center flex items-center">Address book home page</div>
);

export default Home;
